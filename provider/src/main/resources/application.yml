server:
  port: 8762

spring:
  application:
    name: provider

  cloud:
    nacos:
      discovery:
        server-addr: 127.0.0.1:8848

  zipkin:
    sender:
      type: web
    service:
      name: consumer
  sleuth:
    sampler:
      probability: 1
